import { defaultStyles } from '@/styles'
import { Text, View } from 'react-native'

const PlaylistScreen = () => {
	return (
		<View style={defaultStyles.container}>
			<Text style={defaultStyles.text}>PlaylistScreen</Text>
		</View>
	)
}

export default PlaylistScreen
