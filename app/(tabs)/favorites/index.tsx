import { defaultStyles } from '@/styles'
import { Text, View } from 'react-native'

const FavoriteScreen = () => {
	return (
		<View style={defaultStyles.container}>
			<Text style={defaultStyles.text}>FavoriteScreen</Text>
		</View>
	)
}

export default FavoriteScreen
